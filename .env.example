# Example environment variables for DoodleX
# Copy this file to .env and fill in your actual values.
# DO NOT COMMIT YOUR ACTUAL .env FILE TO GIT!

# --- Database Configuration ---
# Used by Postgres container and backend services
DB_USER=postgres
DB_PASSWORD=your_secure_database_password
DB_NAME=postgres
DATABASE_URL=postgresql://postgres:your_secure_database_password@postgres:5432/postgres

# --- Application Secrets ---
# Used by backend and WebSocket services for JWT signing/verification
JWT_SECRET=generate_a_strong_random_secret_key_here

# Used by frontend (build-time and runtime) and potentially backend for Google services
GOOGLE_API_KEY=your_google_cloud_or_ai_studio_api_key

# --- Frontend Configuration ---
# Public URLs used by the frontend application
NEXT_PUBLIC_BACKEND_URL=https://your_domain.com/api/v1
NEXT_PUBLIC_WS_URL=wss://your_domain.com/ws

# --- Node Environment ---
# Typically 'development' or 'production'
# NODE_ENV=production